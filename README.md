# HouseHub
CSE-115
--------------
Firebase relies on cocoapods 
  To install cocoapods do 
    `$ sudo gem install cocoapods`
  Then to install firebase dependencies
    cd house hub
    pod install
  Then open the house hub.xcworkspace in xcode
  From here you can run the code in an iphone 11 simulator
  
